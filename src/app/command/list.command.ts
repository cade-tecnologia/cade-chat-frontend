export const ACTION_COMMAND: Object = {
    ls: (command: string) => {
        
    },
    mkdir: (command: string) => {

    },
    help: (command: string) => {

    },
    date: (command: string) => {
        
    },
}
